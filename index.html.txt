<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EngiConvert | Precision Unit Tool</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="app-wrapper">
        <header>
            <h1>Engi<span class="accent">Convert</span></h1>
            <p>High-precision unit conversion for engineering applications.</p>
        </header>

        <main class="grid-container">
            <section class="converter-card">
                <div class="category-tabs">
                    <label>Select Dimension</label>
                    <select id="unitCategory">
                        <option value="length">Length (Distance)</option>
                        <option value="mass">Mass (Weight)</option>
                        <option value="temp">Temperature</option>
                    </select>
                </div>

                <div class="conversion-ui">
                    <div class="input-block">
                        <input type="number" id="fromInput" placeholder="0.000" step="any">
                        <select id="fromUnit"></select>
                    </div>

                    <div class="separator">to</div>

                    <div class="input-block">
                        <input type="number" id="toInput" readonly placeholder="Result">
                        <select id="toUnit"></select>
                    </div>
                </div>

                <div class="actions">
                    <button id="saveBtn" class="btn-primary">Save to Favorites</button>
                </div>
            </section>

            <aside class="saved-panel">
                <h3>Recent Favorites</h3>
                <ul id="savedList">
                    </ul>
            </aside>
        </main>
    </div>
    <script src="script.js"></script>
</body>
</html>